import 'package:flutter/material.dart';

class AppColors {
  // Colores principales (Gama oscura premium, estilo IDE)
  static const Color background = Color(0xFF0D1117); // Fondo principal (estilo GitHub Dark)
  static const Color surface = Color(0xFF161B22); // Paneles y tarjetas
  static const Color surfaceHighlight = Color(0xFF21262D); // Hover/Highlight
  
  // Colores de acento (Neón para dar vida y gamificación)
  static const Color primary = Color(0xFF58A6FF); // Azul principal (Botones, links)
  static const Color secondary = Color(0xFF3FB950); // Verde (Éxito, correcto, run)
  static const Color error = Color(0xFFF85149); // Rojo (Errores, bugs)
  static const Color warning = Color(0xFFD29922); // Amarillo (Advertencias, XP)
  static const Color accent = Color(0xFFA371F7); // Púrpura (Niveles, logros especiales)

  // Texto
  static const Color textPrimary = Color(0xFFC9D1D9);
  static const Color textSecondary = Color(0xFF8B949E);
}
